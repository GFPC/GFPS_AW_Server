# GFP: только из этого репозитория, в режиме editable (-e).
# Не используйте «pip install aw-server» с PyPI — там другой пакет (upstream ActivityWatch).
#
# Рекомендуемая установка (обходит баг setuptools 80+ с aw_datastore на Linux):
#   pip install -U pip wheel
#   pip install --no-cache-dir -e ./aw-core --config-settings editable_mode=compat
#   pip install --no-cache-dir -e ./aw-server --config-settings editable_mode=compat
# или: bash scripts/install_editable_compat.sh
#
# Простой вариант (если ImportError Datastore не воспроизводится):
-e ./aw-core
-e ./aw-server
